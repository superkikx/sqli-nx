import React from "react";

import "./gui.css";

/* eslint-disable-next-line */
export interface GuiProps {}

export const Gui = (props: GuiProps) => {
  return (
    <div>
      <h1>Welcome to gui component!</h1>
    </div>
  );
};

export default Gui;
